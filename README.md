# Selenium-Java

### webdriver is an interface with empty body where it can have similar methods from any browser (chromedriver,geckodriver(firefoxdriver) , edgedriver etc)

webDriver driver = new ChromeDriver();


## close()

close method closes the currently opened window

## quit()

quit method closes all opened multiple windows(i.e closes browser)

