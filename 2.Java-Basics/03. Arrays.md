# Arrays

An array is a fixed-size, indexed, ordered collection of elements of the same data type

### Key Characteristics of Arrays

**Fixed size:** Once created, size cannot change

**Indexed**: Index starts at 0

**Homogeneous**: All elements must be same data type

Stored in contiguous memory: Fast access (O(1))

Can store primitives or objects

### Declaring arrays
```
int[] arr;   // recommended
int arr2[];  // also valid
```

## creating arrays

### 1. Declare+ create

```
int[] arr = new int[5];
```

### 2.Declare + Create + Initialize
```
int[] arr ={10,20,30,40,50};
```

## Types of Arrays

🔹 **Single-Dimensional**

```
int[] marks = {70, 80, 90};
```

🔹 **Two-Dimensional** (Tables / Grids)
```
int[][] matrix = {
  {1, 2},
  {3, 4}
};
```

System.out.println(matrix[1][0]); // prints 3


## Common Array Operations 

**Searching** (linear, binary)

**Sorting** (Arrays.sort())

**Copying** (Arrays.copyOf())

**Comparing** (Arrays.equals())

**Converting** to **List** (Arrays.asList())


# programs

## 1.Reverse an integer array

```
package arraysPractice;

import java.util.Arrays;

public class ReverseAnIntArray {

	public static void main(String[] args) {
		int[] arr= {10,20,30,40,50,60};
		int i=0, j=arr.length-1,temp;
		while(i<j) {
			temp=arr[i];
			arr[i]=arr[j];
			arr[j]=temp;
			i++;
			j--;
		}
		System.out.println("reversed array is : " );
		for(int num:arr) {
			System.out.print(num + " ");
		}
		

	}

}
```

### output:

```
reversed array is : 
60 50 40 30 20 10
 ```

## 2.check array is sorted or not

```
public static void main(String[] args) {
		int[] arr= {10,20,30,40,50,60};
		
		boolean issorted= true;
		for(int i=0;i<arr.length-1;i++) {
			if(arr[i]>arr[i+1]) {
				issorted=false;
				break;
			}
		}
		System.out.println(issorted);
```

**output:** true
